<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/skysenselogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/skysenselogo"
        android:supportsRtl="true"
        android:theme="@style/Theme.SkySense"
        tools:targetApi="31">
        <activity
            android:name=".termsConditions"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.SkySense.NoActionBar" />
        <activity
            android:name=".splashscreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="false">

            <!-- Otras configuraciones de actividad, si las hay -->
        </activity>
        <activity
            android:name=".checkActivity"
            android:exported="false">

            <!-- Otras configuraciones de actividad, si las hay -->
        </activity>
        <activity
            android:name=".AuthActivity"
            android:exported="false">

            <!-- Otras configuraciones de actividad, si las hay -->
        </activity>
    </application>

</manifest>